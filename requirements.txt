requests>=2.31.0
numpy>=1.26.0
scipy>=1.11.0
python-dotenv>=1.0.0
# Phase 3 — auto-submit (installed by CI only when KICKTIPP_EMAIL is set):
# playwright>=1.40.0  ← not in base requirements; installed separately in predict.yml
